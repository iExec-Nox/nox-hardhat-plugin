packages:
  - "packages/**"

# Allow these dependencies to run their (native binary) build scripts.
# pnpm 11 fails the install (ERR_PNPM_IGNORED_BUILDS) otherwise, breaking CI.
allowBuilds:
  esbuild: true
  unrs-resolver: true
